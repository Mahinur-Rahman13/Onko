#include<iostream>
int main()
{
    //Need to define the co-efficients first
    //Define the transforming equations
    //For each time step, solve for the whole space, first the mechanical equation, then the pressure equation
    //iterate untill covergence
    //iterate for the whole time 
    return 0; 
}